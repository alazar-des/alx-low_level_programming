/**
 * mul - return the multiplication result of the two input numbers
 * @a: first input integer
 * @b: second input integer
 *
 * Return: multiplication result
 */

int mul(int a, int b)
{
	return (a * b);
}
