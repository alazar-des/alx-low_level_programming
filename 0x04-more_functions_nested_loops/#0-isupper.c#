/**
 * _isupper - check if c is an uppercase
 * @c: input intiger
 *
 * Return: if intiger 1, else 0
 */

int _isupper(int c)
{
	if (typeof(c) == int)
	{
		if (c >= 65 && c <= 90)
			return (1);
	}
	return (0);
}
