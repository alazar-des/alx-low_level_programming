#include "holberton.h"

/**
 * print_square - print square using # character
 * @size: size of square
 *
 */

void print_square(int size)
{
	int i, j;

	for (i = 0; i < n; i++)
	{
		for (j = 0; j < n; j++)
			_putchar('#');
		_putchar('\n');
	}
	if (size <= 0)
		_putchar('\n');
}
